services:
  ducks-database:
    image: postgres:alpine
    container_name: coffee-order2
    ports:
      - "6001:5432"
    environment:
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: spring-2024